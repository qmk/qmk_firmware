# List of all the board related files.
BOARDSRC = $(BOARD_PATH)/boards/LG_Keyboard/board.c

# Required include directories
BOARDINC = $(BOARD_PATH)/boards/LG_Keyboard

ALLCSRC += $(BOARDSRC)
ALLINC  += $(BOARDINC)

import { _ as _export_sfc, c as createElementBlock, o as openBlock, a8 as createStaticVNode } from "./chunks/framework.Cauyuiy8.js";
const __pageData = JSON.parse('{"title":"More Detailed make Instructions","description":"","frontmatter":{},"headers":[],"relativePath":"getting_started_make_guide.md","filePath":"getting_started_make_guide.md","lastUpdated":null}');
const _sfc_main = { name: "getting_started_make_guide.md" };
const _hoisted_1 = /* @__PURE__ */ createStaticVNode('<h1 id="more-detailed-make-instructions" tabindex="-1">More Detailed <code>make</code> Instructions <a class="header-anchor" href="#more-detailed-make-instructions" aria-label="Permalink to &quot;More Detailed `make` Instructions&quot;">​</a></h1><p>The full syntax of the <code>make</code> command is <code>&lt;keyboard&gt;:&lt;keymap&gt;:&lt;target&gt;</code>, where:</p><ul><li><code>&lt;keyboard&gt;</code> is the path of the keyboard, for example <code>planck</code><ul><li>Use <code>all</code> to compile all keyboards</li><li>Specify the path to compile a revision, for example <code>planck/rev4</code> or <code>planck/rev3</code></li><li>If the keyboard doesn&#39;t have any folders, it can be left out</li><li>To compile the default folder, you can leave it out</li></ul></li><li><code>&lt;keymap&gt;</code> is the name of the keymap, for example <code>algernon</code><ul><li>Use <code>all</code> to compile all keymaps</li></ul></li><li><code>&lt;target&gt;</code> will be explained in more detail below.</li></ul><p>The <code>&lt;target&gt;</code> means the following</p><ul><li>If no target is given, then it&#39;s the same as <code>all</code> below</li><li><code>all</code> compiles as many keyboard/revision/keymap combinations as specified. For example, <code>make planck/rev4:default</code> will generate a single .hex, while <code>make planck/rev4:all</code> will generate a hex for every keymap available to the planck.</li><li><code>flash</code>, <code>dfu</code>, <code>teensy</code>, <code>avrdude</code>, <code>dfu-util</code>, or <code>bootloadhid</code> compile and upload the firmware to the keyboard. If the compilation fails, then nothing will be uploaded. The programmer to use depends on the keyboard. For most keyboards it&#39;s <code>dfu</code>, but for ChibiOS keyboards you should use <code>dfu-util</code>, and <code>teensy</code> for standard Teensys. To find out which command you should use for your keyboard, check the keyboard specific readme. Visit the <a href="./flashing">Flashing Firmware</a> guide for more details of the available bootloaders. <ul><li><strong>Note</strong>: some operating systems need privileged access for these commands to work. This means that you may need to setup <a href="./faq_build#linux-udev-rules"><code>udev rules</code></a> to access these without root access, or to run the command with root access (<code>sudo make planck/rev4:default:flash</code>).</li></ul></li><li><code>clean</code>, cleans the build output folders to make sure that everything is built from scratch. Run this before normal compilation if you have some unexplainable problems.</li><li><code>distclean</code> removes .hex files and .bin files.</li></ul><p>The following targets are for developers:</p><ul><li><code>show_path</code> shows the path of the source and object files.</li><li><code>dump_vars</code> dumps the makefile variable.</li><li><code>objs-size</code> displays the size of individual object files.</li><li><code>show_build_options</code> shows the options set in &#39;rules.mk&#39;.</li><li><code>check-md5</code> displays the md5 checksum of the generated binary file.</li></ul><p>You can also add extra options at the end of the make command line, after the target</p><ul><li><code>make COLOR=false</code> - turns off color output</li><li><code>make SILENT=true</code> - turns off output besides errors/warnings</li><li><code>make VERBOSE=true</code> - outputs all of the gcc stuff (not interesting, unless you need to debug)</li><li><code>make VERBOSE_LD_CMD=yes</code> - execute the ld command with the -v option.</li><li><code>make VERBOSE_AS_CMD=yes</code> - execute the as command with the -v option.</li><li><code>make VERBOSE_C_CMD=&lt;c_source_file&gt;</code> - add the -v option when compiling the specified C source file.</li><li><code>make DUMP_C_MACROS=&lt;c_source_file&gt;</code> - dump preprocessor macros when compiling the specified C source file.</li><li><code>make DUMP_C_MACROS=&lt;c_source_file&gt; &gt; &lt;logfile&gt;</code> - dump preprocessor macros to <code>&lt;logfile&gt;</code> when compiling the specified C source file.</li><li><code>make VERBOSE_C_INCLUDE=&lt;c_source_file&gt;</code> - dumps the file names to be included when compiling the specified C source file.</li><li><code>make VERBOSE_C_INCLUDE=&lt;c_source_file&gt; 2&gt; &lt;logfile&gt;</code> - dumps the file names to be included to <code>&lt;logfile&gt;</code> when compiling the specified C source file.</li></ul><p>The make command itself also has some additional options, type <code>make --help</code> for more information. The most useful is probably <code>-jx</code>, which specifies that you want to compile using more than one CPU, the <code>x</code> represents the number of CPUs that you want to use. Setting that can greatly reduce the compile times, especially if you are compiling many keyboards/keymaps. I usually set it to one less than the number of CPUs that I have, so that I have some left for doing other things while it&#39;s compiling. Note that not all operating systems and make versions supports that option.</p><p>Here are some examples commands</p><ul><li><code>make all:all</code> builds everything (all keyboard folders, all keymaps). Running just <code>make</code> from the <code>root</code> will also run this.</li><li><code>make ergodox_infinity:algernon:clean</code> will clean the build output of the Ergodox Infinity keyboard.</li><li><code>make planck/rev4:default:flash COLOR=false</code> builds and uploads the keymap without color output.</li></ul><h2 id="rules-mk-options" tabindex="-1"><code>rules.mk</code> Options <a class="header-anchor" href="#rules-mk-options" aria-label="Permalink to &quot;`rules.mk` Options&quot;">​</a></h2><p>Set these variables to <code>no</code> to disable them, and <code>yes</code> to enable them.</p><p><code>BOOTMAGIC_ENABLE</code></p><p>This allows you to hold a key (usually Escape by default) to reset the EEPROM settings that persist over power loss and ready your keyboard to accept new firmware.</p><p><code>MOUSEKEY_ENABLE</code></p><p>This gives you control over cursor movements and clicks via keycodes/custom functions.</p><p><code>EXTRAKEY_ENABLE</code></p><p>This allows you to use the system and audio control key codes.</p><p><code>CONSOLE_ENABLE</code></p><p>This allows you to print messages that can be read using <a href="https://www.pjrc.com/teensy/hid_listen.html" target="_blank" rel="noreferrer"><code>hid_listen</code></a>.</p><p>By default, all debug (<em>dprint</em>) print (<em>print</em>, <em>xprintf</em>), and user print (<em>uprint</em>) messages will be enabled. This will eat up a significant portion of the flash and may make the keyboard .hex file too big to program.</p><p>To disable debug messages (<em>dprint</em>) and reduce the .hex file size, include <code>#define NO_DEBUG</code> in your <code>config.h</code> file.</p><p>To disable print messages (<em>print</em>, <em>xprintf</em>) and user print messages (<em>uprint</em>) and reduce the .hex file size, include <code>#define NO_PRINT</code> in your <code>config.h</code> file.</p><p>To disable print messages (<em>print</em>, <em>xprintf</em>) and <strong>KEEP</strong> user print messages (<em>uprint</em>), include <code>#define USER_PRINT</code> in your <code>config.h</code> file (do not also include <code>#define NO_PRINT</code> in this case).</p><p>To see the text, open <code>hid_listen</code> and enjoy looking at your printed messages.</p><p><strong>NOTE:</strong> Do not include <em>uprint</em> messages in anything other than your keymap code. It must not be used within the QMK system framework. Otherwise, you will bloat other people&#39;s .hex files.</p><p><code>COMMAND_ENABLE</code></p><p>This enables magic commands, typically fired with the default magic key combo <code>LSHIFT+RSHIFT+KEY</code>. Magic commands include turning on debugging messages (<code>MAGIC+D</code>) or temporarily toggling NKRO (<code>MAGIC+N</code>).</p><p><code>SLEEP_LED_ENABLE</code></p><p>Enables your LED to breath while your computer is sleeping. Timer1 is being used here. This feature is largely unused and untested, and needs updating/abstracting.</p><p><code>NKRO_ENABLE</code></p><p>This allows the keyboard to tell the host OS that up to 248 keys are held down at once (default without NKRO is 6). NKRO is off by default, even if <code>NKRO_ENABLE</code> is set. NKRO can be forced by adding <code>#define FORCE_NKRO</code> to your config.h or by binding <code>MAGIC_TOGGLE_NKRO</code> to a key and then hitting the key.</p><p><code>BACKLIGHT_ENABLE</code></p><p>This enables the in-switch LED backlighting. You can specify the backlight pin by putting this in your <code>config.h</code>:</p><pre><code>#define BACKLIGHT_PIN B7\n</code></pre><p><code>MIDI_ENABLE</code></p><p>This enables MIDI sending and receiving with your keyboard. To enter MIDI send mode, you can use the keycode <code>MI_ON</code>, and <code>MI_OFF</code> to turn it off. This is a largely untested feature, but more information can be found in the <code>quantum/quantum.c</code> file.</p><p><code>UNICODE_ENABLE</code></p><p>This allows you to send Unicode characters using <code>UC(&lt;code point&gt;)</code> in your keymap. Code points up to <code>0x7FFF</code> are supported. This covers characters for most modern languages, as well as symbols, but it doesn&#39;t cover emoji.</p><p><code>UNICODEMAP_ENABLE</code></p><p>This allows you to send Unicode characters using <code>UM(&lt;map index&gt;)</code> in your keymap. You will need to maintain a mapping table in your keymap file. All possible code points (up to <code>0x10FFFF</code>) are supported.</p><p><code>UCIS_ENABLE</code></p><p>This allows you to send Unicode characters by inputting a mnemonic corresponding to the character you want to send. You will need to maintain a mapping table in your keymap file. All possible code points (up to <code>0x10FFFF</code>) are supported.</p><p>For further details, as well as limitations, see the <a href="./features/unicode">Unicode page</a>.</p><p><code>AUDIO_ENABLE</code></p><p>This allows you output audio on the C6 pin (needs abstracting). See the <a href="./features/audio">audio page</a> for more information.</p><p><code>VARIABLE_TRACE</code></p><p>Use this to debug changes to variable values, see the <a href="./unit_testing#tracing-variables">tracing variables</a> section of the Unit Testing page for more information.</p><p><code>KEY_LOCK_ENABLE</code></p><p>This enables <a href="./features/key_lock">key lock</a>.</p><p><code>SPLIT_KEYBOARD</code></p><p>This enables split keyboard support (dual MCU like the let&#39;s split and bakingpy&#39;s boards) and includes all necessary files located at quantum/split_common</p><p><code>SPLIT_TRANSPORT</code></p><p>As there is no standard split communication driver for ARM-based split keyboards yet, <code>SPLIT_TRANSPORT = custom</code> must be used for these. It will prevent the standard split keyboard communication code (which is AVR-specific) from being included, allowing a custom implementation to be used.</p><p><code>CUSTOM_MATRIX</code></p><p>Lets you replace the default matrix scanning routine with your own code. For further details, see the <a href="./custom_matrix">Custom Matrix page</a>.</p><p><code>DEBOUNCE_TYPE</code></p><p>Lets you replace the default key debouncing routine with an alternative one. If <code>custom</code> you will need to provide your own implementation.</p><p><code>DEFERRED_EXEC_ENABLE</code></p><p>Enables deferred executor support -- timed delays before callbacks are invoked. See <a href="./custom_quantum_functions#deferred-execution">deferred execution</a> for more information.</p><h2 id="customizing-makefile-options-on-a-per-keymap-basis" tabindex="-1">Customizing Makefile Options on a Per-Keymap Basis <a class="header-anchor" href="#customizing-makefile-options-on-a-per-keymap-basis" aria-label="Permalink to &quot;Customizing Makefile Options on a Per-Keymap Basis&quot;">​</a></h2><p>If your keymap directory has a file called <code>rules.mk</code> any options you set in that file will take precedence over other <code>rules.mk</code> options for your particular keyboard.</p><p>So let&#39;s say your keyboard&#39;s <code>rules.mk</code> has <code>BACKLIGHT_ENABLE = yes</code>. You want your particular keyboard to not have the backlight, so you make a file called <code>rules.mk</code> and specify <code>BACKLIGHT_ENABLE = no</code>.</p>', 65);
const _hoisted_66 = [
  _hoisted_1
];
function _sfc_render(_ctx, _cache, $props, $setup, $data, $options) {
  return openBlock(), createElementBlock("div", null, _hoisted_66);
}
const getting_started_make_guide = /* @__PURE__ */ _export_sfc(_sfc_main, [["render", _sfc_render]]);
export {
  __pageData,
  getting_started_make_guide as default
};

import { _ as _export_sfc, c as createElementBlock, o as openBlock, a8 as createStaticVNode } from "./chunks/framework.Clpp4x2N.js";
const __pageData = JSON.parse('{"title":"EEPROM Driver Configuration","description":"","frontmatter":{},"headers":[],"relativePath":"drivers/eeprom.md","filePath":"drivers/eeprom.md"}');
const _sfc_main = { name: "drivers/eeprom.md" };
const _hoisted_1 = /* @__PURE__ */ createStaticVNode('<h1 id="eeprom-driver-configuration" tabindex="-1">EEPROM Driver Configuration <a class="header-anchor" href="#eeprom-driver-configuration" aria-label="Permalink to &quot;EEPROM Driver Configuration {#eeprom-driver-configuration}&quot;">​</a></h1><p>The EEPROM driver can be swapped out depending on the needs of the keyboard, or whether extra hardware is present.</p><p>Selecting the EEPROM driver is done in your keyboard&#39;s <code>rules.mk</code>:</p><table><thead><tr><th>Driver</th><th>Description</th></tr></thead><tbody><tr><td><code>EEPROM_DRIVER = vendor</code> (default)</td><td>Uses the on-chip driver provided by the chip manufacturer. For AVR, this is provided by avr-libc. This is supported on ARM for a subset of chips -- STM32F3xx, STM32F1xx, and STM32F072xB will be emulated by writing to flash. STM32L0xx and STM32L1xx will use the onboard dedicated true EEPROM. Other chips will generally act as &quot;transient&quot; below.</td></tr><tr><td><code>EEPROM_DRIVER = i2c</code></td><td>Supports writing to I2C-based 24xx EEPROM chips. See the driver section below.</td></tr><tr><td><code>EEPROM_DRIVER = spi</code></td><td>Supports writing to SPI-based 25xx EEPROM chips. See the driver section below.</td></tr><tr><td><code>EEPROM_DRIVER = transient</code></td><td>Fake EEPROM driver -- supports reading/writing to RAM, and will be discarded when power is lost.</td></tr><tr><td><code>EEPROM_DRIVER = wear_leveling</code></td><td>Frontend driver for the wear_leveling system, allowing for EEPROM emulation on top of flash -- both in-MCU and external SPI NOR flash.</td></tr></tbody></table><h2 id="vendor-eeprom-driver-configuration" tabindex="-1">Vendor Driver Configuration <a class="header-anchor" href="#vendor-eeprom-driver-configuration" aria-label="Permalink to &quot;Vendor Driver Configuration {#vendor-eeprom-driver-configuration}&quot;">​</a></h2><h4 id="stm32l0l1-eeprom-driver-configuration" tabindex="-1">STM32 L0/L1 Configuration <a class="header-anchor" href="#stm32l0l1-eeprom-driver-configuration" aria-label="Permalink to &quot;STM32 L0/L1 Configuration {#stm32l0l1-eeprom-driver-configuration}&quot;">​</a></h4><div class="warning custom-block"><p class="custom-block-title">WARNING</p><p>Resetting EEPROM using an STM32L0/L1 device takes up to 1 second for every 1kB of internal EEPROM used.</p></div><table><thead><tr><th><code>config.h</code> override</th><th>Description</th><th>Default Value</th></tr></thead><tbody><tr><td><code>#define STM32_ONBOARD_EEPROM_SIZE</code></td><td>The size of the EEPROM to use, in bytes. Erase times can be high, so it&#39;s configurable here, if not using the default value.</td><td>Minimum required to cover base <em>eeconfig</em> data, or <code>1024</code> if VIA is enabled.</td></tr></tbody></table><h2 id="i2c-eeprom-driver-configuration" tabindex="-1">I2C Driver Configuration <a class="header-anchor" href="#i2c-eeprom-driver-configuration" aria-label="Permalink to &quot;I2C Driver Configuration {#i2c-eeprom-driver-configuration}&quot;">​</a></h2><p>Currently QMK supports 24xx-series chips over I2C. As such, requires a working i2c_master driver configuration. You can override the driver configuration via your config.h:</p><table><thead><tr><th><code>config.h</code> override</th><th>Description</th><th>Default Value</th></tr></thead><tbody><tr><td><code>#define EXTERNAL_EEPROM_I2C_BASE_ADDRESS</code></td><td>Base I2C address for the EEPROM -- shifted left by 1 as per i2c_master requirements</td><td>0b10100000</td></tr><tr><td><code>#define EXTERNAL_EEPROM_I2C_ADDRESS(addr)</code></td><td>Calculated I2C address for the EEPROM</td><td><code>(EXTERNAL_EEPROM_I2C_BASE_ADDRESS)</code></td></tr><tr><td><code>#define EXTERNAL_EEPROM_BYTE_COUNT</code></td><td>Total size of the EEPROM in bytes</td><td>8192</td></tr><tr><td><code>#define EXTERNAL_EEPROM_PAGE_SIZE</code></td><td>Page size of the EEPROM in bytes, as specified in the datasheet</td><td>32</td></tr><tr><td><code>#define EXTERNAL_EEPROM_ADDRESS_SIZE</code></td><td>The number of bytes to transmit for the memory location within the EEPROM</td><td>2</td></tr><tr><td><code>#define EXTERNAL_EEPROM_WRITE_TIME</code></td><td>Write cycle time of the EEPROM, as specified in the datasheet</td><td>5</td></tr><tr><td><code>#define EXTERNAL_EEPROM_WP_PIN</code></td><td>If defined the WP pin will be toggled appropriately when writing to the EEPROM.</td><td><em>none</em></td></tr></tbody></table><p>Some I2C EEPROM manufacturers explicitly recommend against hardcoding the WP pin to ground. This is in order to protect the eeprom memory content during power-up/power-down/brown-out conditions at low voltage where the eeprom is still operational, but the i2c master output might be unpredictable. If a WP pin is configured, then having an external pull-up on the WP pin is recommended.</p><p>Default values and extended descriptions can be found in <code>drivers/eeprom/eeprom_i2c.h</code>.</p><p>Alternatively, there are pre-defined hardware configurations for available chips/modules:</p><table><thead><tr><th>Module</th><th>Equivalent <code>#define</code></th><th>Source</th></tr></thead><tbody><tr><td>CAT24C512 EEPROM</td><td><code>#define EEPROM_I2C_CAT24C512</code></td><td><a href="https://www.sparkfun.com/products/14764" target="_blank" rel="noreferrer">https://www.sparkfun.com/products/14764</a></td></tr><tr><td>RM24C512C EEPROM</td><td><code>#define EEPROM_I2C_RM24C512C</code></td><td><a href="https://www.sparkfun.com/products/14764" target="_blank" rel="noreferrer">https://www.sparkfun.com/products/14764</a></td></tr><tr><td>24LC32A EEPROM</td><td><code>#define EEPROM_I2C_24LC32A</code></td><td><a href="https://www.microchip.com/en-us/product/24LC32A" target="_blank" rel="noreferrer">https://www.microchip.com/en-us/product/24LC32A</a></td></tr><tr><td>24LC64 EEPROM</td><td><code>#define EEPROM_I2C_24LC64</code></td><td><a href="https://www.microchip.com/en-us/product/24LC64" target="_blank" rel="noreferrer">https://www.microchip.com/en-us/product/24LC64</a></td></tr><tr><td>24LC128 EEPROM</td><td><code>#define EEPROM_I2C_24LC128</code></td><td><a href="https://www.microchip.com/en-us/product/24LC128" target="_blank" rel="noreferrer">https://www.microchip.com/en-us/product/24LC128</a></td></tr><tr><td>24LC256 EEPROM</td><td><code>#define EEPROM_I2C_24LC256</code></td><td><a href="https://www.sparkfun.com/products/525" target="_blank" rel="noreferrer">https://www.sparkfun.com/products/525</a></td></tr><tr><td>MB85RC256V FRAM</td><td><code>#define EEPROM_I2C_MB85RC256V</code></td><td><a href="https://www.adafruit.com/product/1895" target="_blank" rel="noreferrer">https://www.adafruit.com/product/1895</a></td></tr></tbody></table><div class="tip custom-block"><p class="custom-block-title">TIP</p><p>If you find that the EEPROM is not cooperating, ensure you&#39;ve correctly shifted up your EEPROM address by 1. For example, the datasheet might state the address as <code>0b01010000</code> -- the correct value of <code>EXTERNAL_EEPROM_I2C_BASE_ADDRESS</code> needs to be <code>0b10100000</code>.</p></div><h2 id="spi-eeprom-driver-configuration" tabindex="-1">SPI Driver Configuration <a class="header-anchor" href="#spi-eeprom-driver-configuration" aria-label="Permalink to &quot;SPI Driver Configuration {#spi-eeprom-driver-configuration}&quot;">​</a></h2><p>Currently QMK supports 25xx-series chips over SPI. As such, requires a working spi_master driver configuration. You can override the driver configuration via your config.h:</p><table><thead><tr><th><code>config.h</code> override</th><th>Default Value</th><th>Description</th></tr></thead><tbody><tr><td><code>#define EXTERNAL_EEPROM_SPI_SLAVE_SELECT_PIN</code></td><td><em>none</em></td><td>SPI Slave select pin in order to inform that the EEPROM is currently being addressed</td></tr><tr><td><code>#define EXTERNAL_EEPROM_SPI_CLOCK_DIVISOR</code></td><td><code>64</code></td><td>Clock divisor used to divide the peripheral clock to derive the SPI frequency</td></tr><tr><td><code>#define EXTERNAL_EEPROM_BYTE_COUNT</code></td><td><code>8192</code></td><td>Total size of the EEPROM in bytes</td></tr><tr><td><code>#define EXTERNAL_EEPROM_PAGE_SIZE</code></td><td><code>32</code></td><td>Page size of the EEPROM in bytes, as specified in the datasheet</td></tr><tr><td><code>#define EXTERNAL_EEPROM_ADDRESS_SIZE</code></td><td><code>2</code></td><td>The number of bytes to transmit for the memory location within the EEPROM</td></tr></tbody></table><p>Default values and extended descriptions can be found in <code>drivers/eeprom/eeprom_spi.h</code>.</p><p>Alternatively, there are pre-defined hardware configurations for available chips/modules:</p><table><thead><tr><th>Module</th><th>Equivalent <code>#define</code></th><th>Source</th></tr></thead><tbody><tr><td>MB85RS64V FRAM</td><td><code>define EEPROM_SPI_MB85RS64V</code></td><td><a href="https://www.adafruit.com/product/1897" target="_blank" rel="noreferrer">https://www.adafruit.com/product/1897</a></td></tr></tbody></table><div class="warning custom-block"><p class="custom-block-title">WARNING</p><p>There&#39;s no way to determine if there is an SPI EEPROM actually responding. Generally, this will result in reads of nothing but zero.</p></div><h2 id="transient-eeprom-driver-configuration" tabindex="-1">Transient Driver configuration <a class="header-anchor" href="#transient-eeprom-driver-configuration" aria-label="Permalink to &quot;Transient Driver configuration {#transient-eeprom-driver-configuration}&quot;">​</a></h2><p>The only configurable item for the transient EEPROM driver is its size:</p><table><thead><tr><th><code>config.h</code> override</th><th>Description</th><th>Default Value</th></tr></thead><tbody><tr><td><code>#define TRANSIENT_EEPROM_SIZE</code></td><td>Total size of the EEPROM storage in bytes</td><td>64</td></tr></tbody></table><p>Default values and extended descriptions can be found in <code>drivers/eeprom/eeprom_transient.h</code>.</p><h2 id="wear_leveling-eeprom-driver-configuration" tabindex="-1">Wear-leveling Driver Configuration <a class="header-anchor" href="#wear_leveling-eeprom-driver-configuration" aria-label="Permalink to &quot;Wear-leveling Driver Configuration {#wear_leveling-eeprom-driver-configuration}&quot;">​</a></h2><p>The wear-leveling driver uses an algorithm to minimise the number of erase cycles on the underlying MCU flash memory.</p><p>There is no specific configuration for this driver, but the wear-leveling system used by this driver may need configuration. See the <a href="#wear_leveling-configuration">wear-leveling configuration</a> section for more information.</p><h1 id="wear_leveling-configuration" tabindex="-1">Wear-leveling Configuration <a class="header-anchor" href="#wear_leveling-configuration" aria-label="Permalink to &quot;Wear-leveling Configuration {#wear_leveling-configuration}&quot;">​</a></h1><p>The wear-leveling driver has a few possible <em>backing stores</em> that may be used by adding to your keyboard&#39;s <code>rules.mk</code> file:</p><table><thead><tr><th>Driver</th><th>Description</th></tr></thead><tbody><tr><td><code>WEAR_LEVELING_DRIVER = embedded_flash</code></td><td>This driver is used for emulating EEPROM by writing to embedded flash on the MCU.</td></tr><tr><td><code>WEAR_LEVELING_DRIVER = spi_flash</code></td><td>This driver is used to address external SPI NOR Flash peripherals.</td></tr><tr><td><code>WEAR_LEVELING_DRIVER = rp2040_flash</code></td><td>This driver is used to write to the same storage the RP2040 executes code from.</td></tr><tr><td><code>WEAR_LEVELING_DRIVER = legacy</code></td><td>This driver is the &quot;legacy&quot; emulated EEPROM provided in historical revisions of QMK. Currently used for STM32F0xx and STM32F4x1, but slated for deprecation and removal once <code>embedded_flash</code> support for those MCU families is complete.</td></tr></tbody></table><div class="warning custom-block"><p class="custom-block-title">WARNING</p><p>All wear-leveling drivers require an amount of RAM equivalent to the selected logical EEPROM size. Increasing the size to 32kB of EEPROM requires 32kB of RAM, which a significant number of MCUs simply do not have.</p></div><h2 id="wear_leveling-efl-driver-configuration" tabindex="-1">Wear-leveling Embedded Flash Driver Configuration <a class="header-anchor" href="#wear_leveling-efl-driver-configuration" aria-label="Permalink to &quot;Wear-leveling Embedded Flash Driver Configuration {#wear_leveling-efl-driver-configuration}&quot;">​</a></h2><p>This driver performs writes to the embedded flash storage embedded in the MCU. In most circumstances, the last few of sectors of flash are used in order to minimise the likelihood of collision with program code.</p><p>Configurable options in your keyboard&#39;s <code>config.h</code>:</p><table><thead><tr><th><code>config.h</code> override</th><th>Default</th><th>Description</th></tr></thead><tbody><tr><td><code>#define WEAR_LEVELING_EFL_FIRST_SECTOR</code></td><td><em>unset</em></td><td>The first sector on the MCU to use. By default this is not defined and calculated at runtime based on the MCU. However, different flash sizes on MCUs may require custom configuration.</td></tr><tr><td><code>#define WEAR_LEVELING_EFL_FLASH_SIZE</code></td><td><em>unset</em></td><td>Allows overriding the flash size available for use for wear-leveling. Under normal circumstances this is automatically calculated and should not need to be overridden. Specifying a size larger than the amount actually available in flash will usually prevent the MCU from booting.</td></tr><tr><td><code>#define WEAR_LEVELING_EFL_OMIT_LAST_SECTOR_COUNT</code></td><td><code>0</code></td><td>Number of sectors to omit at the end of the flash. These sectors will not be allocated to the driver and the usable flash block will be offset, but keeping the set flash size. Useful on devices with bootloaders requiring a check flag at the end of flash to be present in order to confirm a valid, bootable firmware.</td></tr><tr><td><code>#define WEAR_LEVELING_LOGICAL_SIZE</code></td><td><code>(backing_size/2)</code></td><td>Number of bytes &quot;exposed&quot; to the rest of QMK and denotes the size of the usable EEPROM.</td></tr><tr><td><code>#define WEAR_LEVELING_BACKING_SIZE</code></td><td><code>2048</code></td><td>Number of bytes used by the wear-leveling algorithm for its underlying storage, and needs to be a multiple of the logical size.</td></tr><tr><td><code>#define BACKING_STORE_WRITE_SIZE</code></td><td><em>automatic</em></td><td>The byte width of the underlying write used on the MCU, and is usually automatically determined from the selected MCU family. If an error occurs in the auto-detection, you&#39;ll need to consult the MCU&#39;s datasheet and determine this value, specifying it directly.</td></tr></tbody></table><div class="warning custom-block"><p class="custom-block-title">WARNING</p><p>If your MCU does not boot after swapping to the EFL wear-leveling driver, it&#39;s likely that the flash size is incorrectly detected, usually as an MCU with larger flash and may require overriding.</p></div><h2 id="wear_leveling-flash_spi-driver-configuration" tabindex="-1">Wear-leveling SPI Flash Driver Configuration <a class="header-anchor" href="#wear_leveling-flash_spi-driver-configuration" aria-label="Permalink to &quot;Wear-leveling SPI Flash Driver Configuration {#wear_leveling-flash_spi-driver-configuration}&quot;">​</a></h2><p>This driver performs writes to an external SPI NOR Flash peripheral. It also requires a working configuration for the SPI NOR Flash peripheral -- see the <a href="./flash">flash driver</a> documentation for more information.</p><p>Configurable options in your keyboard&#39;s <code>config.h</code>:</p><table><thead><tr><th><code>config.h</code> override</th><th>Default</th><th>Description</th></tr></thead><tbody><tr><td><code>#define WEAR_LEVELING_EXTERNAL_FLASH_BLOCK_COUNT</code></td><td><code>1</code></td><td>Number of blocks in the external flash used by the wear-leveling algorithm.</td></tr><tr><td><code>#define WEAR_LEVELING_EXTERNAL_FLASH_BLOCK_OFFSET</code></td><td><code>0</code></td><td>The index first block in the external flash used by the wear-leveling algorithm.</td></tr><tr><td><code>#define WEAR_LEVELING_LOGICAL_SIZE</code></td><td><code>((block_count*block_size)/2)</code></td><td>Number of bytes &quot;exposed&quot; to the rest of QMK and denotes the size of the usable EEPROM. Result must be &lt;= 64kB.</td></tr><tr><td><code>#define WEAR_LEVELING_BACKING_SIZE</code></td><td><code>(block_count*block_size)</code></td><td>Number of bytes used by the wear-leveling algorithm for its underlying storage, and needs to be a multiple of the logical size.</td></tr><tr><td><code>#define BACKING_STORE_WRITE_SIZE</code></td><td><code>8</code></td><td>The write width used whenever a write is performed on the external flash peripheral.</td></tr></tbody></table><div class="warning custom-block"><p class="custom-block-title">WARNING</p><p>There is currently a limit of 64kB for the EEPROM subsystem within QMK, so using a larger flash is not going to be beneficial as the logical size cannot be increased beyond 65536. The backing size may be increased to a larger value, but erase timing may suffer as a result.</p></div><h2 id="wear_leveling-rp2040-driver-configuration" tabindex="-1">Wear-leveling RP2040 Driver Configuration <a class="header-anchor" href="#wear_leveling-rp2040-driver-configuration" aria-label="Permalink to &quot;Wear-leveling RP2040 Driver Configuration {#wear_leveling-rp2040-driver-configuration}&quot;">​</a></h2><p>This driver performs writes to the same underlying storage that the RP2040 executes its code.</p><p>Configurable options in your keyboard&#39;s <code>config.h</code>:</p><table><thead><tr><th><code>config.h</code> override</th><th>Default</th><th>Description</th></tr></thead><tbody><tr><td><code>#define WEAR_LEVELING_RP2040_FLASH_SIZE</code></td><td><code>PICO_FLASH_SIZE_BYTES</code></td><td>Number of bytes of flash on the board.</td></tr><tr><td><code>#define WEAR_LEVELING_RP2040_FLASH_BASE</code></td><td><code>(flash_size-sector_size)</code></td><td>The byte-wise location that the backing storage should be located.</td></tr><tr><td><code>#define WEAR_LEVELING_LOGICAL_SIZE</code></td><td><code>(backing_size/2)</code></td><td>Number of bytes &quot;exposed&quot; to the rest of QMK and denotes the size of the usable EEPROM.</td></tr><tr><td><code>#define WEAR_LEVELING_BACKING_SIZE</code></td><td><code>8192</code></td><td>Number of bytes used by the wear-leveling algorithm for its underlying storage, and needs to be a multiple of the logical size as well as the sector size.</td></tr><tr><td><code>#define BACKING_STORE_WRITE_SIZE</code></td><td><code>2</code></td><td>The write width used whenever a write is performed on the external flash peripheral.</td></tr></tbody></table><h2 id="wear_leveling-legacy-driver-configuration" tabindex="-1">Wear-leveling Legacy EEPROM Emulation Driver Configuration <a class="header-anchor" href="#wear_leveling-legacy-driver-configuration" aria-label="Permalink to &quot;Wear-leveling Legacy EEPROM Emulation Driver Configuration {#wear_leveling-legacy-driver-configuration}&quot;">​</a></h2><p>This driver performs writes to the embedded flash storage embedded in the MCU much like the normal Embedded Flash Driver, and is only for use with STM32F0xx and STM32F4x1 devices. This flash implementation is still currently provided as the EFL driver is currently non-functional for the previously mentioned families.</p><p>By default, <code>1024</code> bytes of emulated EEPROM is provided:</p><table><thead><tr><th>MCU</th><th>EEPROM Provided</th><th>Flash Used</th></tr></thead><tbody><tr><td>STM32F042</td><td><code>1024</code> bytes</td><td><code>2048</code> bytes</td></tr><tr><td>STM32F070</td><td><code>1024</code> bytes</td><td><code>2048</code> bytes</td></tr><tr><td>STM32F072</td><td><code>1024</code> bytes</td><td><code>2048</code> bytes</td></tr><tr><td>STM32F401</td><td><code>1024</code> bytes</td><td><code>16384</code> bytes</td></tr><tr><td>STM32F411</td><td><code>1024</code> bytes</td><td><code>16384</code> bytes</td></tr></tbody></table><p>Under normal circumstances configuration of this driver requires intimate knowledge of the MCU&#39;s flash structure -- reconfiguration is at your own risk and will require referring to the code.</p>', 53);
const _hoisted_54 = [
  _hoisted_1
];
function _sfc_render(_ctx, _cache, $props, $setup, $data, $options) {
  return openBlock(), createElementBlock("div", null, _hoisted_54);
}
const eeprom = /* @__PURE__ */ _export_sfc(_sfc_main, [["render", _sfc_render]]);
export {
  __pageData,
  eeprom as default
};

if type guix &> /dev/null; then
    use guix_shell make python arm-none-eabi-toolchain avr-toolchain avrdude dfu-programmer dfu-util
    layout python3
    export AVR_CFLAGS="-isystem $CROSS_OBJC_INCLUDE_PATH"
elif type nix &> /dev/null; then
    use nix
    layout python
fi

[tool.poetry]
name = "scripts"
version = "0.1.0"
description = ""
authors = ["Smierx"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.10"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
